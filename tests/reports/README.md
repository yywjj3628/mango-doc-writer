# tests/reports/ README

## 目录用途

存放实际运行测试后生成的报告文件。

每次测试运行后，将实际输出写入此目录，与 `expected/` 中的预期输出对比。

## 命名规范

| 文件名 | 说明 |
|--------|------|
| `{测试编号}-actual.json` | 实际运行输出 |
| `{测试编号}-diff.json` | 与预期输出的差异 |
| `{测试编号}-summary.md` | 测试通过/失败总结 |

## 当前状态

报告目录将在阶段 12（最小调用入口）完成后，实际运行测试时填充。
